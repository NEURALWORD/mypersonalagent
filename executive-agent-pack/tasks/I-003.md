# Task — I-003 — Google Calendar OAuth + sync

**Phase**: 3 | **Effort**: 3h | **Deps**: F-007

## Goal
OAuth + initial backfill (90d past, 60d future) + incremental sync via push channels.

## Acceptance criteria
- [ ] OAuth flow with calendar.readonly + calendar.events scopes
- [ ] Initial backfill into `raw_items` (source='gcal')
- [ ] Push channel registration; webhook updates `raw_items` on changes
- [ ] Sync token persisted for incremental
- [ ] Renewal cron (channels expire after 7d)
- [ ] Tests: backfill, incremental update, channel renewal
