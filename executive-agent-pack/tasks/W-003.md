# Task — W-003 — Wisdom retrieval API

**Phase**: 11 | **Effort**: 3h | **Deps**: W-001

## Goal
Per-user contextualized wisdom retrieval. Used by WisdomAgent.

## Acceptance criteria
- [ ] `getWisdom(userId, domain, context)` returns 0-3 relevant insights
- [ ] Filters by user's cohort_signature
- [ ] Filters by relevance to context (semantic match on domain)
- [ ] Each insight includes: statement, sample_n, confidence_interval, source_label
- [ ] Read-only access enforced via separate DB role
- [ ] Tests: cohort filtering, relevance ranking
