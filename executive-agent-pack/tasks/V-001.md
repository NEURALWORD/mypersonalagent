# Task — V-001 — LiveKit room + voice agent worker

**Phase**: 7 | **Effort**: 4h | **Deps**: F-001 | **Spec**: spec/voice.md

## Goal
LiveKit Cloud setup; per-user persistent room; `apps/voice` worker process.

## Acceptance criteria
- [ ] LiveKit project created, env vars configured
- [ ] `apps/voice/src/worker.ts` connects as participant to user rooms
- [ ] Room naming: `voice:user:{userId}`
- [ ] Worker subscribes to audio tracks, publishes synthesized audio
- [ ] Token endpoint in `apps/web` issues short-lived JWTs for client connect
- [ ] Tests: token validity, worker can connect to room
