# Task — F-007 — Clerk auth

**Phase**: 0 | **Effort**: 1-2h | **Deps**: F-006

## Goal
Clerk auth: email + Google + passkey. Sync Clerk user → `users` table.

## Acceptance criteria
- [ ] Clerk middleware protects `/app/*`
- [ ] Webhook at `/api/clerk/webhook` upserts user row on `user.created`/`user.updated`
- [ ] Sign-in pages styled to match dark theme
- [ ] `useUser()` available client-side; `currentUser()` server-side
- [ ] tRPC `protectedProcedure` derives `userId` from Clerk session
- [ ] Test: webhook signature verification, user creation flow
