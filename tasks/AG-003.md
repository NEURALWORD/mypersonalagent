# Task — AG-003 — Calendar Agent

**Phase**: 4 | **Effort**: 5h | **Deps**: AG-001, I-004

## Goal
prepareBrief, findMeetingTime, summarizeMeeting, assessMeetingNecessity.

## Acceptance criteria
- [ ] `prepareBrief(meetingId)` 1-page brief with attendee context, agenda guess, recent thread, action items pending from prior, suggested questions
- [ ] `findMeetingTime(constraints, attendees)` — for now, internal scheduling (works on user's calendar only). External handled later via A2A.
- [ ] `summarizeMeeting(meetingId, transcript?)` — extract action items + decisions; works with optional transcript
- [ ] `assessMeetingNecessity(meetingId)` returns enum: necessary / could_be_email / could_be_shorter
- [ ] All tools registered with master orchestrator
- [ ] Eval ≥ 50 cases per task
