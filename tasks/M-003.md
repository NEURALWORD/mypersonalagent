# Task — M-003 — MemoryOrchestrator: remember (write path)

**Phase**: 1 | **Effort**: 4h | **Deps**: M-001, M-002, AI-001 | **Spec**: spec/memory-layer.md

## Goal
`remember(input)` per spec: dedup, confidence merge, contradiction linking, entity update, idempotency, cache invalidation.

## Acceptance criteria
- [ ] `MemoryOrchestrator.remember(input)` matches spec signature
- [ ] Embed via router (text-embedding-3-small)
- [ ] Dedup: cosine ≥ 0.95 → merge confidence, do not duplicate
- [ ] Contradiction: cosine 0.8-0.95 with semantic opposition (LLM judge) → create link of type 'contradicts'
- [ ] Idempotency on (content+source+ts) hash
- [ ] Update entity `lastInteraction` for tagged entities
- [ ] Recompute `importance` for affected entities (EMA)
- [ ] Invalidate Redis cache for user
- [ ] Emit Inngest event `memory.created`
- [ ] Tests: simple write, dedup-merge, contradiction creation, idempotent re-call
