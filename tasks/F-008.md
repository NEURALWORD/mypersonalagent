# Task — F-008 — tRPC v11 setup

**Phase**: 0 | **Effort**: 1-2h | **Deps**: F-006, F-007

## Goal
tRPC v11 server + client, type-safe end-to-end. SuperJSON. React Query integration.

## Acceptance criteria
- [ ] `apps/web/src/server/trpc.ts` with `t.router`, `protectedProcedure`, `publicProcedure`
- [ ] Context includes `userId`, `db`, `req`
- [ ] Routers: `auth`, `chat`, `memory`, `actions`, `integrations`, `settings`
- [ ] Each router minimal placeholder until later phases fill them
- [ ] Client `lib/trpc.ts` provider in root layout
- [ ] One example mutation + query working end-to-end (e.g. `settings.get`/`settings.update`)
- [ ] Errors mapped via `AppError` from `@exec/shared/errors`
