# Task — I-001 — Gmail OAuth + watch

**Phase**: 3 | **Effort**: 4h | **Deps**: F-007, F-005

## Goal
OAuth connect Gmail; subscribe to push notifications via Gmail watch + Pub/Sub.

## Acceptance criteria
- [ ] OAuth flow (incremental scopes: read + modify + send)
- [ ] Tokens stored encrypted in `integrations` table
- [ ] Refresh handler with auto-renew
- [ ] Watch endpoint registered against Gmail API
- [ ] Pub/Sub webhook at `/api/integrations/gmail/webhook` validates JWT, enqueues `gmail.message.received` Inngest event
- [ ] Disconnect flow revokes token + clears watch
- [ ] Tests: token refresh, webhook signature verification
