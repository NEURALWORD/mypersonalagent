# Task — F-010 — Observability (Langfuse + Sentry + PostHog)

**Phase**: 0 | **Effort**: 2h | **Deps**: F-006

## Goal
Three pillars wired: LLM tracing (Langfuse), error tracking (Sentry), product analytics (PostHog).

## Acceptance criteria
- [ ] `packages/ai/tracing.ts` exports Langfuse client + helpers (startTrace, recordSpan, end)
- [ ] Sentry initialized in `apps/web` (server + client) with source maps
- [ ] PostHog client + server SDK; auto-capture on web app
- [ ] User identification: PostHog `identify()` on auth, hash userId for non-prod
- [ ] PII scrubber: filter known fields (email body, message content) from Sentry breadcrumbs
- [ ] Test: throw error in tRPC, verify Sentry receives; emit event, verify PostHog
- [ ] Test: dummy LLM call goes through router, Langfuse trace appears
