# Task — AG-004 — People Agent

**Phase**: 4 | **Effort**: 5h | **Deps**: AG-001, I-005

## Goal
Network intelligence: profile, path-finding, enrichment trigger, stale detection.

## Acceptance criteria
- [ ] `getProfile(entityId)` returns rich profile + recent interactions + signals
- [ ] `findPath(fromUser, toEntityId)` BFS up to depth 3 over relationships
- [ ] `enrichProfile(entityId)` triggers I-005 enrichment + waits for result
- [ ] `staleRelationships(thresholdDays, minImportance)` — ranked list
- [ ] Tools registered with master orchestrator
- [ ] Eval: profile completeness, path correctness on synthetic graph
