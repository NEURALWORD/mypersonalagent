# Task — AG-007 — Wisdom Agent

**Phase**: 4 | **Effort**: 4h | **Deps**: W-001, W-002, W-003 | **Spec**: spec/wisdom.md

## Goal
Personalized wisdom retrieval with strict privacy enforcement.

## Acceptance criteria
- [ ] `findRelevantPatterns(input)` returns insights with cohort + sample_n + CI
- [ ] Reads ONLY `wisdom_aggregates` (RLS-enforced — assert in test)
- [ ] Per-user disable check: returns empty if `wisdom_opt_in=false`
- [ ] Surfaces via proactive (P-002) when contextually relevant
- [ ] Tools registered with master orchestrator
- [ ] Tests: privacy enforcement (cannot read raw cross-user data), opt-out behavior
