# Task — F-009 — Inngest

**Phase**: 0 | **Effort**: 1h | **Deps**: F-006

## Goal
Inngest Cloud + dev server. First cron + first event.

## Acceptance criteria
- [ ] `packages/jobs/src/client.ts` exports configured Inngest client
- [ ] Route `/api/inngest` registered with all functions
- [ ] First cron: `daily-budget-rollover` at 00:00 UTC, resets `user_daily_budget`
- [ ] First event: `memory.created` → triggers consolidation candidate check
- [ ] Inngest dev server starts via `pnpm dev:inngest`
- [ ] Function failure handling: retries, then dead-letter via `job_runs` table
